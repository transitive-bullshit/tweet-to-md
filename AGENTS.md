# Project: Tweet to Markdown

## Conventions

- use `pnpm`
- use modern typescript (esm, no commonjs)
- no semicolons
- oxfmt for formatting (`pnpm format`)
- use the `ky` NPM module as a wrapper around `fetch` for HTTP requests
- use `zod` v4 for validating external data
